#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
    &kp 1    &kp 2    &kp 3    &kp 4     &kp 5     &mo 1

            >;
        };

        bt_layer {
            bindings = <
    &bootloader    &bt BT_CLR    &bt BT_SEL 0    &bt BT_SEL 1     &bt BT_SEL 2     &none

            >;
        };
    };
};